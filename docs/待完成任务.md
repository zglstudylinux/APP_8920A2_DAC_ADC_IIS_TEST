# BT8920A2 任务状态总览（更新版）

> **目的**：开启新 Claude Code 会话后，能快速看出哪些任务已经完成、哪些没完成、当前状态和下一步。
> **最新更新**：**A4 任务完整通过**（双板联调，IIS_SLAVE_RAMRX + DMA + 库回调 `iis_rx_process_test` 推 DAC FIFO，4 根杜邦线接好即可听声）；A3 同步改 44.1K 与 A4 对齐；A2/A1 早已完成。B/C/D 待开始，DAC 模拟输出降噪待专项处理。

---

## ✅ 已完成任务

### 任务 A1：DAC 输出 500/1k/2k Hz 正弦波（✅ 完成）

- 工程基线 1 kHz（PC1，8 kHz + sine8k1k）；
- 改采样率到 16 kHz（PC2，验证 SPR_16000 + DACDIGCON0=0x219）；
- 模拟音量调到 50（PC3，avol=50 → N_4DB=0x02，-4 dB）；
- 替换为 32 点/周期 500 Hz 表（PC4）；
- 加 16 点/1 kHz 表 + 8 点/2 kHz 表（PC5/PC6，用 `A1_CUR_FREQ` 编译时宏切换）。

**关键文件**：
- [app/platform/bsp/bsp_sys.c](../../app/platform/bsp/bsp_sys.c)（test_pcm2dac）
- [docs/SDK入门与任务实施指南.md 第 21-24 章](../docs/SDK入门与任务实施指南.md)

---

### 任务 A2：AUX ADC → DAC 重写 auxadc_pcm_to_dac（✅ 完成）

- 在应用层用 GCC `--wrap=` 链接器选项覆盖库强符号（PC7）；
- AUX 输入引脚修正为 **PB1/PB2**（AUXL/AUXR 源 1，**不是默认的 PA6/PA7**，也不是早期 A2 baseline 的 PE6/PE7 — 后者已让给 IIS_G2）；
- 放开 `aux_analog_channel_select_ext()` 中 PB1/PB2 路径（写 AUANGCON7/8 BIT(9)）；
- `dac_obuf_init()` 末尾加 `AUBUFCON &= ~BIT(0)` 退出 FIFO reset；
- 多次降噪调优：ADC 采样率 = DAC 采样率 = **SPR_16000**（避免 SRC 引入不稳定）；
- `auxadc_cb.samples = 512`（降低 ISR 触发频率 ~31 Hz）；
- `auxadc_cb.gain = (8<<6)|15`（降低总增益避免削顶）；
- `dac_set_avol(53)`（接近上限 N_1DB = -1 dB，声音大）。

**关键文件**：
- [app/bsp_ext/bsp_adc_pcm_to_dac_ext.c](../../app/bsp_ext/bsp_adc_pcm_to_dac_ext.c)（`__wrap_auxadc_pcm_to_dac`）
- [app/bsp_ext/bsp_adc_aux_ext.c](../../app/bsp_ext/bsp_adc_aux_ext.c)（channel = 0x22, samples = 512）
- [app/bsp_ext/bsp_dac_ext.c](../../app/bsp_ext/bsp_dac_ext.c)（AUBUFCON reset 修复）
- [app/projects/standard/app.cbp](../../app/projects/standard/app.cbp)（`--wrap=auxadc_pcm_to_dac`）
- [docs/SDK入门与任务实施指南.md 第 25 章](../docs/SDK入门与任务实施指南.md)
- [docs/A2任务降噪调优教学.md](../docs/A2任务降噪调优教学.md)（零基础小白版教学）

---

## ⏳ 未开始任务

### 任务 A3：AUX → IIS Master SRCTX（✅ 完成）

**原文**：`iis_master_srctx_init()` 在库中，需要在应用层重写。可用逻辑分析仪抓 BCLK/LRC/DO 三个 IO 验证，或对接到另一颗芯片 `TEST_IISRX2DAC`。

**当前状态**：已完成。
- 在应用层重写 `iis_master_srctx_init()`（用 `--wrap=iis_master_srctx_init` 链接器选项，确认链接生效）；
- DAC + IIS SRCTX 同步输出（AUX 音频同时走 DAC 和 IIS，已验证）；
- 逻辑分析仪验证：BCLK=1.667MHz / LRC=47.85kHz / DO 数据波形正确；
- I2S 协议解码：左右声道数据 = AUX 1 kHz 正弦波；
- **分支**：`new_minimax_zgl01`；
- **关键文件**：
  - [app/bsp_ext/bsp_iis_master_ext.c](../../app/bsp_ext/bsp_iis_master_ext.c)（`__wrap_iis_master_srctx_init`）
  - [app/bsp_ext/bsp_adc_aux_ext.c](../../app/bsp_ext/bsp_adc_aux_ext.c)（`test_aux_adc2dac_for_a3()` + `auxadc_param_init_for_a3()`）
  - [app/bsp_ext/bsp_adc_aux_ext.h](../../app/bsp_ext/bsp_adc_aux_ext.h)（A3 专用原型）
  - [app/projects/standard/app.cbp](../../app/projects/standard/app.cbp)（`--wrap=iis_master_srctx_init` 链接器选项 + `bsp_iis_master_ext.c` Unit）
  - [app/projects/standard/main.c](../../app/projects/standard/main.c)（`xcfg_cb.test_mode = TEST_AUX_ADC2IISSRCTX` + `test_aux_adc2dac_for_a3()`，当前默认强制模式为 `TEST_AUX_ADC2DAC`）
  - [docs/A3任务IIS_MASTER_SRCTX教学.md](../docs/A3任务IIS_MASTER_SRCTX教学.md)（完整教学）
- **已知问题**：DAC 模拟输出噪声大，频率跳动 + 毛刺多。SRCTX 数据通路工作正常，是 DAC FIFO/PHASECOM 在 48 kHz 下需要额外调优（待 A3.1 专项）。

---

### 任务 A4：IIS Slave RAMRX → DAC（✅ 完成）

**原文**：`iis_slave_ram_rx_2_dac()` 在库中，需要重写。可用逻辑分析仪发送信号或与 A3 板对接。

**当前状态**：已完成（A3+A4 双板联调成功）。
- 在应用层重写 `iis_slave_ram_rx_2_dac()`（用 `--wrap=iis_slave_ram_rx_2_dac` 链接器选项）；
- 配置 `IIS_SLAVE_RAMRX` (0x0A) + `IIS_G2`（PE5=BCLK in, PE6=LRC in, PB2=DI in, PB1 关 MCLK）；
- DMA 双缓冲（库默认 64 samples/buffer, 2048B 在 `.iis2dac_buf` bram）；
- 复用库回调 `iis_rx_process_test` 推 DAC FIFO（自带 `aubuf_adjust` 调速 + 1s `print_dac_info`）；
- A3+A4 联动：A3 同步改 44.1K（原本 48K），两板 LRC 严格同步；
- 双板联调 4 根线（PE5/PE6/PE7 ↔ PE5/PE6/PB2 + GND）接好即听声；
- **踩坑记录**：`iis_cfg_t` 必须 `static`，否则 wrap() 返回后栈帧释放，DMA 完成 IRQ 触发 `iis_rx_process_test` 访问 `iis_libcfg`（已悬空）→ 读到栈垃圾 0x2323... → CPU 跳到 EPC=0x23232324 → ERR:3/7 反复重启。修复 `static iis_cfg_slave;` 后稳定。
- **分支**：`new_minimax_zgl01`；
- **关键文件**：
  - [app/bsp_ext/bsp_iis_slave_ext.c](../../app/bsp_ext/bsp_iis_slave_ext.c)（`__wrap_iis_slave_ram_rx_2_dac` + `static iis_cfg_slave`）
  - [app/bsp_ext/bsp_iis_slave_ext.h](../../app/bsp_ext/bsp_iis_slave_ext.h)（wrap 原型）
  - [app/bsp_ext/bsp_adc_aux_ext.c](../../app/bsp_ext/bsp_adc_aux_ext.c)（A3 改 44.1K：`SPR_48000 → SPR_44100`）
  - [app/projects/standard/app.cbp](../../app/projects/standard/app.cbp)（`--wrap=iis_slave_ram_rx_2_dac` 链接器选项 + `bsp_iis_slave_ext.c` Unit）
  - [app/projects/standard/main.c](../../app/projects/standard/main.c)（line 42 双板切换：A3 板 `TEST_AUX_ADC2IISSRCTX` / A4 板 `TEST_IISRX2DAC`）
  - [docs/A4任务IIS_SLAVE_RAMRX教学.md](../docs/A4任务IIS_SLAVE_RAMRX教学.md)（完整教学 + 接线图 + 排查表）

---

### 任务 B：SDDAC 驱动实战（⏳ 待开始）

**原文**：建立 `sddac_test` 目录，从寄存器手册独立配置 SDDAC，输出 500/1k/2k Hz；测试 16 级音量调节；记录模拟音量 0 dB → -6 dB 的 Vpp 变化。

**当前状态**：A1 已打通 DAC 输出，可以借用 `dac_init()` 流程参考；但 B 要求**独立从寄存器手册配置**，不走 SDK 封装函数。

**预计步骤**：
1. 新建 `app/bsp_ext/sddac_test/` 子目录；
2. 阅读 8920A2 SDDAC 寄存器手册（SDDACDIGCON/SDDACVOLCON/SDDAC analog CON）；
3. 从裸寄存器开始配（不用 `dac_init()`）；
4. 输出 500/1k/2k Hz；
5. 用两个按键做 16 级音量；
6. 测 Vpp 随 dB 的变化（按任务原文需求）。

---

### 任务 C：SDADC 驱动实战（⏳ 待开始）

**原文**：建立 `sdadc_test` 目录，配置 SDADC，把采样数据经 SDDAC 输出。

**当前状态**：A2 已打通 SDADC 通路（虽然走的是库函数），可以参考。

**预计步骤**：
1. 新建 `sddac_test` 子目录；
2. 从裸寄存器开始配置 SDADC；
3. 采样数据 → DAC 输出（与任务 B 联动）；
4. 模拟任务 B 的音量控制。

---

### 任务 D：IIS 端到端链路（⏳ 待开始）

**原文**：整合 ADC → IIS OUT → IIS IN → DAC。

**当前状态**：依赖 A3/A4 完成后才能做。

**预计步骤**：
1. 完成 A3（IIS OUT）；
2. 完成 A4（IIS IN → DAC）；
3. 整合单芯片内 ADC → DAC + DAC → IIS OUT → 外部 IIS IN → DAC 的全链路；
4. 验证端到端频率和相位一致性。

---

## 🔧 当前工程状态

**测试模式强制切换**：[app/projects/standard/main.c](../../app/projects/standard/main.c) 当前强制 `xcfg_cb.test_mode = TEST_AUX_ADC2IISSRCTX`（A3 板默认）。A4 板烧录时改为 `TEST_IISRX2DAC`（取消注释另一行）。

**关键代码位置**：
- `app/projects/standard/main.c`：入口和测试模式分派
- `app/platform/bsp/bsp_sys.c`：A1 测试 + 编译器宏 `A1_CUR_FREQ`
- `app/bsp_ext/bsp_dac_ext.c`：DAC 配置实现
- `app/bsp_ext/bsp_adc_aux_ext.c`：AUX ADC（PB1/PB2，A2/A3 共用）
- `app/bsp_ext/bsp_adc_pcm_to_dac_ext.c`：`__wrap_auxadc_pcm_to_dac`
- `app/bsp_ext/bsp_iis_ext.h`：IIS 类型定义（A3/A4 已用 `iis_cfg_init()` 库 API 实现）
- `app/projects/standard/app.cbp`：`--wrap=auxadc_pcm_to_dac` 链接器选项

**构建命令**：
- `prebuild.bat`：`riscv32-elf-xmaker -b res.xm` + `riscv32-elf-xmaker -b xcfg.xm`
- 编译：`riscv32-elf-gcc`（Code::Blocks 自动）
- 链接：ld 通过 `ram.o` 链接脚本
- `postbuild.bat`：`objcopy` 生成 `app.bin`，再生成 `app.dcf` 烧录格式

---

## 📚 文档索引

| 文档 | 内容 |
|---|---|
| [docs/SDK入门与任务实施指南.md](SDK入门与任务实施指南.md) | 工程文档（详细技术参考）|
| [docs/BT8920A2音频实验教学.md](BT8920A2音频实验教学.md) | 零基础小白教学（A1 完整）|
| [docs/A2任务降噪调优教学.md](A2任务降噪调优教学.md) | A2 零基础小白教学（含降噪调优）|
| [docs/A3任务IIS_MASTER_SRCTX教学.md](A3任务IIS_MASTER_SRCTX教学.md) | A3 零基础小白教学（IIS 主机 SRCTX + DAC 44.1K）|
| [docs/A4任务IIS_SLAVE_RAMRX教学.md](A4任务IIS_SLAVE_RAMRX教学.md) | A4 零基础小白教学（IIS 从机 RAMRX + 双板联调）|
| [docs/I2S驱动资料.md](I2S驱动资料.md) | 530X IIS 示例（A3/A4 参考）|
| [docs/reference/sine_16k_500hz.wav](reference/sine_16k_500hz.wav) | A1 参考音频 |
| [docs/待完成任务.md](待完成任务.md) | 本文档（任务状态）|

---

## 🚀 下一步推荐路径

```
A1 (DONE) → A2 (DONE) → A3 (重写 iis_master_srctx_init) → A4 (重写 iis_slave_ram_rx_2_dac) → D (端到端链路)
                                                       ↘
                                                       B (SDDAC) ↔ C (SDADC)
```

**推荐**：A1/A2/A3/A4 全部完成。下一步建议做 **B（SDDAC 实战）** 或 **D（IIS 端到端单芯片内链路）**。

---

## 📋 Git 提交历史（最近 6 次）

```
3bd257d fix(a4): make iis_cfg_slave static to fix ERR:3/7 crash
6a91b5c feat(a4): add IIS_SLAVE_RAMRX wrap + sync A3 to 44.1 kHz
0e422cf feat: switch default test to TEST_AUX_ADC2DAC on PB1/PB2 pins
9e454d9 feat(a3): rewrite iis_master_srctx_init via --wrap, DAC 48K + IIS_G2 SRCTX
837112f docs: refresh README to reflect A1 + A2 completion
50d67e0 chore: catch-up A1 step-3 macros and reference WAV
```

---

**任务状态更新时间**：A4 双板联调通过后。