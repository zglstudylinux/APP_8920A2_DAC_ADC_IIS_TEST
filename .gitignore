# RISC-V 工具链产物 / Build output
# 注: Output/bin/ 现在纳入版本控制, 方便下次构建直接 git clone + Build
#     (用户 2026-07-27 明确要求, 配合 A3+A4 双板联调成果固化)

# 临时编译产物(中间文件, 不需要跟踪)
*.dep
*.elf
*.lst
*.out
*.bak
*.layout
*.depend

# 中间 obj (最终链接产物 app.* 已经跟踪)
*.o
*.obj

# Code::Blocks 私有文件（保留项目文件 .cbp）
# （*.layout / *.depend 已上面）

# IDE/编辑器临时文件
*.swp
*~
.vscode/
.idea/
*.user

# OS
Thumbs.db
.DS_Store

# 大型 SDK 静态库（按需选择是否忽略；本仓库保留可帮助本地重复构建）
# *.a

# 本地验证日志（除非要提交对比基线）
logs/
*.log
*.txt.log

# ★ Output/bin/ 全部纳入版本控制
#   - app.bin / app.dcf / app.rv32  最终烧录镜像
#   - map.txt                       链接 map, 排查崩溃有用
#   - xcfg.* / res.*                配置/资源生成物
#   - prebuild.bat / postbuild.bat  构建脚本
#   - res/                          资源数据
#   - Settings                      工程配置
#   上面的 *.o / *.obj 等通用规则依然排除中间 obj